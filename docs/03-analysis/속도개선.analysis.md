# 속도개선 (Speed Improvement) Analysis Report

> **Analysis Type**: Gap Analysis (Implementation Verification)
>
> **Project**: BusDriver
> **Analyst**: bkit-gap-detector
> **Date**: 2026-03-02
> **Design Doc**: docs/02-design/features/속도개선.design.md (NOT FOUND)

---

## 1. Analysis Overview

### 1.1 Analysis Purpose

Verify that the "속도개선" speed improvement feature has been correctly implemented across two axes:

1. **Perceived Speed** -- Navigation overlay with dynamic messages, loading feedback on link clicks and bottom-nav tab transitions.
2. **Actual Speed** -- Next.js `fetch` tag-based caching on all data-layer read queries, with `revalidateTag` calls in every mutation action, and removal of `force-dynamic` from routes page.

### 1.2 Analysis Scope

Since no formal design document (`docs/02-design/features/속도개선.design.md`) exists, the 12-item implementation specification provided by the developer is treated as the de-facto design. Each item is verified against the actual source code.

- **Implementation Paths**:
  - `components/navigation-overlay.tsx`
  - `components/link-with-loading.tsx`
  - `components/layout/mobile-bottom-nav.tsx`
  - `lib/supabase/rest.ts`
  - `lib/data/school.ts`, `lib/data/student.ts`, `lib/data/route.ts`, `lib/data/payment.ts`
  - `app/(protected)/schools/actions.ts`
  - `app/(protected)/routes/actions.ts`
  - `app/(protected)/dashboard/actions.ts`
  - `app/(protected)/routes/page.tsx`
- **Analysis Date**: 2026-03-02

---

## 2. Gap Analysis (Design vs Implementation)

### 2.1 Perceived Speed -- Navigation Overlay & Loading Feedback

| # | Design Item | Implementation File | Status | Evidence |
|---|-------------|---------------------|--------|----------|
| 1 | `show(message?)` accepts optional message | `components/navigation-overlay.tsx:9,28-29` | ✅ Match | `show: (msg?: string) => { const m = msg ?? '잠시만 기다려 주세요.'; ... }` |
| 2 | `loadingMessage` prop with default `'이동 중...'` | `components/link-with-loading.tsx:11,14` | ✅ Match | `loadingMessage = '이동 중...'` in props, passed to `show(loadingMessage)` |
| 3 | Loading overlay on bottom-nav tab clicks | `components/layout/mobile-bottom-nav.tsx:42,91` | ✅ Match | `NavTab.onClick` calls `show('이동 중...')` when `!active`; FAB also calls `show('이동 중...')` |

### 2.2 Actual Speed -- Cache Infrastructure

| # | Design Item | Implementation File | Status | Evidence |
|---|-------------|---------------------|--------|----------|
| 4 | `restSelect` supports `next: { tags }` option | `lib/supabase/rest.ts:14,34` | ✅ Match | `opts?.next?: { revalidate?: number \| false; tags?: string[] }` -- spread into fetch options |
| 5 | `getSchools()` uses `tags: ['schools']` | `lib/data/school.ts:18` | ✅ Match | `next: { tags: ['schools'] }` |
| 6a | `getStudentsBySchool` uses `tags: ['students']` | `lib/data/student.ts:45` | ✅ Match | `next: { tags: ['students'] }` |
| 6b | `getAllStudents` uses `tags: ['students']` | `lib/data/student.ts:59` | ✅ Match | `next: { tags: ['students'] }` |
| 6c | `getStudentsByParent` uses `tags: ['students']` | `lib/data/student.ts:52` | ✅ Match | `next: { tags: ['students'] }` |
| 7a | `getRoutesBySchool` uses `tags: ['routes']` (routes) | `lib/data/route.ts:81` | ✅ Match | `next: { tags: ['routes'] }` |
| 7b | `getRoutesBySchool` uses `tags: ['routes']` (route_stops) | `lib/data/route.ts:84` | ✅ Match | `next: { tags: ['routes'] }` on sub-query too |
| 8a | `getPaymentsByStudent` uses `tags: ['payments']` | `lib/data/payment.ts:65` | ✅ Match | `next: { tags: ['payments'] }` |
| 8b | `getPaymentsBySchool` uses `tags: ['payments']` | `lib/data/payment.ts:84` | ✅ Match | `next: { tags: ['payments'] }` |
| 8c | `getAllPayments` uses `tags: ['payments']` | `lib/data/payment.ts:129` | ✅ Match | `next: { tags: ['payments'] }` |

### 2.3 Cache Invalidation -- revalidateTag Coverage

| # | Design Item | Implementation File | Status | Evidence |
|---|-------------|---------------------|--------|----------|
| 9a | schools/actions: `revalidateTag('schools')` | `app/(protected)/schools/actions.ts:63,77,119,141` | ✅ Match | Called in create, update, updateForm, delete |
| 9b | schools/actions: `revalidateTag('students')` | `app/(protected)/schools/actions.ts:142,187,204,232,262,389` | ✅ Match | Called in delete, createStudent, updateStatus, assign, unassign, updateInfo |
| 9c | schools/actions: `revalidateTag('payments')` | `app/(protected)/schools/actions.ts:318` | ✅ Match | Called in recordPayment |
| 10a | routes/actions: `revalidateTag('routes')` | `app/(protected)/routes/actions.ts:27,35,42,59,80,104` | ✅ Match | All route mutations |
| 10b | routes/actions: `revalidateTag('students')` | `app/(protected)/routes/actions.ts:60,81` | ✅ Match | assignStudentToRoute, assignBulk |
| 11 | dashboard/actions: `revalidateTag('students')` in changePickupPoint | `app/(protected)/dashboard/actions.ts:117` | ✅ Match | `revalidateTag('students')` |
| 12 | routes/page.tsx: `force-dynamic` removed | `app/(protected)/routes/page.tsx` | ✅ Match | No `export const dynamic` found in file |

### 2.4 Match Rate Summary

```
+---------------------------------------------+
|  Overall Match Rate: 100%                    |
+---------------------------------------------+
|  Total items verified:   18                  |
|  Matched:                18 items (100%)     |
|  Missing in design:      0 items (0%)        |
|  Not implemented:         0 items (0%)       |
+---------------------------------------------+
```

---

## 3. Code Quality Analysis

### 3.1 Strengths

| Area | Observation |
|------|-------------|
| Cache strategy | Consistent tag naming (`schools`, `students`, `routes`, `payments`) across all data files |
| Invalidation coverage | Every server action that mutates data calls the appropriate `revalidateTag` |
| UX feedback | Overlay uses portal, auto-hides on pathname change via `PathnameWatcher`, sub-text adapts to message content |
| Type safety | `restSelect` signature includes typed `next` option with `tags` and `revalidate` |

### 3.2 Observations and Minor Concerns

| Type | File | Location | Description | Severity |
|------|------|----------|-------------|----------|
| Missing cache tags | `lib/data/student.ts` | L39 (getStudentById) | Single-record fetch has no cache tag -- may not benefit from caching | Info |
| Missing cache tags | `lib/data/school.ts` | L33 (getSchoolById) | Single-record fetch has no cache tag | Info |
| Missing cache tags | `lib/data/route.ts` | L62 (getRouteById) | Single-record fetch has no cache tag | Info |
| Missing cache tags | `lib/data/student.ts` | L65 (getUnassignedStudents) | No cache tag on this query | Info |
| Redundant revalidatePath | `app/(protected)/schools/actions.ts` | Multiple | Both `revalidateTag` and `revalidatePath` are called -- `revalidateTag` alone should suffice for cached fetches | Info |
| N+1 fetch pattern | `lib/data/route.ts` | L83-84 | `getRoutesBySchool` runs a sub-query per route for stops | Warning |

### 3.3 Security Issues

No security issues found related to the speed improvement feature.

---

## 4. Performance Analysis

### 4.1 Caching Coverage

| Data Layer Function | Cache Tag | Invalidation Actions | Status |
|---------------------|-----------|----------------------|--------|
| `getSchools()` | `schools` | createSchool, updateSchool(x2), deleteSchool | ✅ Complete |
| `getStudentsBySchool()` | `students` | createStudent, updateStatus, assign, unassign, updateInfo, deleteSchool, assignRoute(x2), changePickup | ✅ Complete |
| `getAllStudents()` | `students` | (same as above) | ✅ Complete |
| `getStudentsByParent()` | `students` | (same as above) | ✅ Complete |
| `getRoutesBySchool()` | `routes` | createRoute, updateRoute, deleteRoute, assignRoute(x2), saveStops, deleteSchool | ✅ Complete |
| `getPaymentsByStudent()` | `payments` | recordPayment, deleteSchool | ✅ Complete |
| `getPaymentsBySchool()` | `payments` | (same as above) | ✅ Complete |
| `getAllPayments()` | `payments` | (same as above) | ✅ Complete |

### 4.2 Potential Bottleneck

| Location | Problem | Impact | Recommendation |
|----------|---------|--------|----------------|
| `lib/data/route.ts:83-84` | N+1 query: fetches route_stops per route in loop | Multiplied latency when school has many routes | Consider batch fetch of all route_stops for a school |
| `lib/data/student.ts:168` | `countStudents()` fetches all rows then `.length` | Wasteful for count-only use case | Consider Supabase COUNT header or `HEAD` request |

---

## 5. Clean Architecture Compliance

### 5.1 Layer Dependency Verification

The project follows a Starter-level architecture (`components`, `lib`, `app`).

| Layer | Expected Dependencies | Actual Dependencies | Status |
|-------|----------------------|---------------------|--------|
| Components (Presentation) | Hooks, Context | `navigation-overlay` context via hook | ✅ |
| Data layer (`lib/data/*`) | `lib/supabase/rest` | Only imports rest utilities + types | ✅ |
| Server Actions (Application) | Data layer, Auth | Imports from `lib/data/*`, `lib/auth/*`, `lib/validation` | ✅ |
| REST infrastructure (`lib/supabase/rest`) | None (standalone) | Only uses `server-only` + native `fetch` | ✅ |

### 5.2 Architecture Score

```
+---------------------------------------------+
|  Architecture Compliance: 100%               |
+---------------------------------------------+
|  Correct layer placement: All files          |
|  Dependency violations:   0                  |
|  Server-only enforcement: Present            |
+---------------------------------------------+
```

---

## 6. Convention Compliance

### 6.1 Naming Convention Check

| Category | Convention | Checked | Compliance | Violations |
|----------|-----------|:-------:|:----------:|------------|
| Components | PascalCase | 4 | 100% | - |
| Functions | camelCase | 30+ | 100% | - |
| Constants | UPPER_SNAKE_CASE | 6 | 100% | `ADMIN_LEFT`, `ADMIN_RIGHT`, `PARENT_TABS`, etc. |
| Files (component) | kebab-case.tsx | 3 | 100% | `navigation-overlay.tsx`, `link-with-loading.tsx`, `mobile-bottom-nav.tsx` |
| Files (utility) | kebab-case.ts | 5 | 100% | `rest.ts`, `school.ts`, etc. |

### 6.2 Import Order Check

- [x] External libraries first (`react`, `next`, `clsx`)
- [x] Internal absolute imports second (`@/components/...`, `@/lib/...`)
- [x] Relative imports third
- [x] Type imports where applicable (`import type`)
- [x] `'server-only'` at top of server files

### 6.3 Convention Score

```
+---------------------------------------------+
|  Convention Compliance: 100%                 |
+---------------------------------------------+
|  Naming:           100%                      |
|  Import Order:     100%                      |
|  File Structure:   100%                      |
+---------------------------------------------+
```

---

## 7. Overall Scores

| Category | Score | Status |
|----------|:-----:|:------:|
| Design Match | 100% | ✅ |
| Architecture Compliance | 100% | ✅ |
| Convention Compliance | 100% | ✅ |
| **Overall** | **100%** | ✅ |

---

## 8. Missing Design Document

The design document at `docs/02-design/features/속도개선.design.md` does not exist. This analysis was performed against the 12-item implementation specification provided inline. To complete the PDCA cycle properly, a retroactive design document should be created covering:

1. Perceived speed strategy (overlay, loading messages)
2. Cache architecture (tag-based caching with `next: { tags }`)
3. Cache invalidation matrix (which actions invalidate which tags)
4. force-dynamic removal rationale

---

## 9. Recommended Actions

### 9.1 Immediate (No blockers found)

No critical issues. All 12 design items are fully implemented.

### 9.2 Short-term Improvements

| Priority | Item | File | Expected Impact |
|----------|------|------|-----------------|
| Low | Add cache tags to single-record fetches (`getStudentById`, `getSchoolById`, `getRouteById`) | `lib/data/student.ts:39`, `school.ts:33`, `route.ts:62` | Consistent caching on detail pages |
| Low | Add cache tag to `getUnassignedStudents()` | `lib/data/student.ts:65` | Cache unassigned student list |
| Low | Fix N+1 in `getRoutesBySchool` | `lib/data/route.ts:83` | Reduced DB round-trips |
| Low | Remove redundant `revalidatePath` calls where `revalidateTag` already covers | `app/(protected)/schools/actions.ts` | Cleaner invalidation logic |

### 9.3 Documentation

| Item | Description |
|------|-------------|
| Create design document | Write `docs/02-design/features/속도개선.design.md` retroactively |
| Cache invalidation matrix | Document which tags are invalidated by which server actions |

---

## 10. Next Steps

- [x] Verify all 12 implementation items -- COMPLETE (18 sub-items, 100% match)
- [ ] Create retroactive design document
- [ ] Consider cache tags for single-record fetches
- [ ] Write completion report (`속도개선.report.md`)

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 1.0 | 2026-03-02 | Initial gap analysis | bkit-gap-detector |
